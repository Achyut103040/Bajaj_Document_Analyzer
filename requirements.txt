# VERCEL SERVERLESS DEPLOYMENT - ESSENTIAL DEPENDENCIES
fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.0
python-dotenv==1.0.0
requests==2.31.0
python-multipart==0.0.6
aiofiles==0.24.0

# Optional lightweight ML (if needed, but may cause size issues)
# scikit-learn==1.3.2
# numpy==1.24.3

# DO NOT ADD TORCH - Causes Vercel deployment failures
# torch is too heavy for serverless deployment